import pandas as pd
import matplotlib.pyplot as plt

# Load your CSV with millisecond timestamps
df = pd.read_csv('DATA.CSV')

sensors = [
     tf,
     voc_index,
     sraw,
     temp,
     pressure,
     NO,
     UV
]
# Convert milliseconds to datetime
#df['ms'] = pd.to_datetime(df['Time'], unit='ms')
#df.set_index('Time', inplace=True)

# Create subplots
fig, axs = plt.subplots(nrows=sensor.length(), ncols=1, figsize=(12, 8), sharex=True)

# Plot each sensor separately
i = 0
for (sensors) as X:

  df['ax'].plot(ax=axs[i], color='blue', title=X)
  axs[i].set_ylabel(X)
  axs[i].grid(True)
  i++

# Common x-label
plt.xlabel('Time')
plt.tight_layout()
plt.show()
